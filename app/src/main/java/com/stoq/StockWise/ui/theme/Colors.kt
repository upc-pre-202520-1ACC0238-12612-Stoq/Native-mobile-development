package com.stoq.StockWise.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Colores compartidos para toda la aplicación StockWise.
 * Centraliza todas las definiciones de colores para evitar duplicaciones.
 */

// Fondos
val CombosBg = Color(0xFFF5E6D3)      // Fondo beige para combos
val DashboardBg = Color(0xFFF5E6D3)   // Fondo beige para dashboard
val CardBg = Color.White

// Colores de texto
val TextDark = Color(0xFF4B3D37)      // Texto oscuro
val TextSecondary = Color(0xFF666666) // Texto secundario gris

// Colores de botones
val ButtonBg = Color(0xFF2196F3)      // Azul principal para botones

// Colores semánticos
val Info = Color(0xFF1E88E5)          // Azul para información
val Danger = Color(0xFFE53935)        // Rojo para errores/danger
val Success = Color(0xFF27AE60)       // Verde para éxito
val WarningColor = Color(0xFFF57C00)  // Naranja para warnings

// Colores alternativos para combos (inspirados en dashboard)
val CombosBgAlt = Color(0xFFF0F8FF)   // Azul muy claro alternativo
val TextDarkAlt = Color(0xFF2C3E50)   // Azul oscuro alternativo
val ButtonBgAlt = Color(0xFF3498DB)   // Azul principal alternativo
val InfoAlt = Color(0xFF3498DB)       // Azul alternativo para información
val DangerAlt = Color(0xFFE74C3C)     // Rojo alternativo para errores
